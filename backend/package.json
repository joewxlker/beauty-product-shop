{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend server to handle http requests and mongoDb databasing for online store user requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w --outDir dist src/index.ts",
    "start": "nodemon dist/index.js"
  },
  "author": "joseph walker",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "express": "^4.18.1",
    "mongodb": "^4.7.0",
    "tsconfig": "^7.0.0"
  }
}
